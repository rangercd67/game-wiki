# 2026-09-16

## 完成
- 清理 `tmp/jcwiki-import/`（与 git 跟踪的 `legacy/jcwiki` 逐字节一致，diff -rq 校验通过），释放 54.5 MB；`tmp/` 现为空。
- 修正上一轮统计错误：`legacy/` 实为 136 文件 / 54.45 MB（中文路径被 git 转义导致此前误报 0）；`.git` 对象库 49.57 MiB。
- 建立项目基线：源码 352 行（gamewiki 包）+ 测试 38 行（仅覆盖 indexer）；无 pyproject.toml / LICENSE / CI / lint 配置。

## 发现（待处理）
- **缺陷**：`web/index.html` 第 67 行 `<aside id="games">` 与第 70 行 `<div id="games">` 重复 ID。`web/app.js:45` 的 `$("#games").innerHTML` 会覆盖整个侧边栏，导致 `#allKeys`/`#kinds` 被销毁，`app.js:52` 抛 TypeError 被 catch 吞掉 → 侧边栏「资料类型」筛选器消失、统计区显示错误。
- **事故**：13:44–13:47 期间，外部数据源 `E:\Games\GameTools` 的内容被写入项目仓库根目录。已从外部源**消失** 6 项：`攻略/`、`药剂工艺：炼金模拟器攻略/`、`P3RE全技能效果及出处一览.xls`、`P5R攻略2.0.rar`、`PSV女神异闻录4黄金版攻略.pdf`、`原神小游戏——熠金妙剂.xlsx`；另有 `P3P&P4G攻略合集包/`、`P5R攻略2.0/` 已复制到仓库但源仍在（疑似传输进行中）。E 盘回收站无对应记录（仅 2023 年旧条目）→ 外部源删除未经回收站。
- 内容未丢（仓库内副本大小与索引快照记录完全一致），但外部源原文已不存在。
- 后果：索引 13,998 条中 **464 条悬空**（172.06 MB）；未跟踪污染面 11 条 / 约 117.55 MB，存在误提交风险。
- 冲突：`.trae/rules/project.md` 第 6 行（外部源只读，不得移动/删除）与第 7 行（不得提交完整副本）；README「源文件保持原位」；`docs/architecture.md`「只读引用，不纳入本仓库」。

## 证据文件（tmp/ 已被 .gitignore 忽略）
- `tmp/incident.txt` — 事故取证（回收站、索引快照比对、逐字节哈希校验）
- `tmp/ongoing.txt` — 传输进行中判定、外部源现存 25 项、未跟踪全量、悬空条目统计
- `tmp/timeline.txt` — 时间戳对照（本会话内两次扫描结果矛盾的原因）
- `tmp/evidence.txt` / `tmp/evidence2.txt`、`tmp/repo_survey*.txt`、`tmp/name_check.txt`

## 已实施（commit c99e0f1）
- 修复侧边栏缺陷：`web/index.html:70` 内层容器 `id="games"` → `id="gameList"`，`web/app.js:45` 选择器同步更新。保留 `#games` 给 `<aside>`（导航锚点 + `styles.css:152` 的 `scroll-margin-top` 都依赖它）。
- 新增 `tests/test_web_assets.py`（65 行，纯标准库）：校验 index.html 无重复 id、app.js 的 `$("#id")`/`$(".class")` 选择器均能解析、引用的 css/js 资源存在。
- 新增空文件 `tests/__init__.py`：此前 `unittest discover -s tests -t .` 因 tests 非包而报 "Start directory is not importable"。
- README 增补「## 测试」小节，给出运行命令。
- 验证方式：用 `git show HEAD:web/*` 取修复前版本，喂给同一套校验逻辑 → 旧版被「重复 id: ['games']」拦下，新版通过；全套 5 个测试 OK。

## 新发现的缺陷（未修）
- `gamewiki/server.py:51-52`：`FileNotFoundError` 统一映射为 503「索引尚未建立，请先运行扫描。」。但 `_preview` 内 `(source/relative).resolve(strict=True)` 对悬空条目同样抛 FileNotFoundError → 用户看到的是误导性提示。需区分「索引库缺失」与「源文件已不存在」。

## 已实施（commit 416fb11）
- 决策：索引源由外部 `E:\Games\GameTools` 改为**仅索引仓库内**；资料统一归入 `library/` 且不纳入 Git。
- 归位：新建 `library/` 并移入 4 个目录 + 4 个散落文件，逐项校验字节数/文件数一致（271 文件 / 213.94 MB），源位置已清空、根目录无残留。移动脚本 `tmp/do_move.py`，报告 `tmp/move_report.txt`。
- `.gitignore` 增 `library/`（第 28 行），117 MB 资料不再出现在 git status。
- `gamewiki/config.py`：新增 `PROJECT_ROOT`，`DEFAULT_SOURCE` → `PROJECT_ROOT / "library"`。
- `README.md` / `docs/architecture.md` 更新数据流与目录边界；architecture.md 新增「已知缺口」小节（PDF/Office 不可预览、索引无增量刷新、悬空记录错误提示不准确）。
- 索引重建：271 条 / 0 跳过 / 0 错误；**悬空由 464 条降为 0 条**。类型分布 image 182 / document 88 / archive 1。
- 端到端实测（临时起服务 + 真实 HTTP，`tmp/e2e_check.py`）：`/api/stats` `/api/games` `/api/files` 均 200；中文关键词「地图」命中 3 条；PNG 预览 200/641 KB；PDF 预览 403「此类型禁止内容预览」；路径穿越被拦（未读到 `config.py`）但返回 503 误导性提示——实证了该缺陷；前端 `/`、`/app.js`、`/styles.css` 均 200。
- 未采纳：原计划给 `files` 表加 `source` 列。理由：`metadata` 表已记录源根，单源下 per-row 冗余；将来加第二个源时本就需改查询，那时再加列更合适。

## 已实施（commit 1e4f9b2）：分支 B — docx/xlsx 预览
- 新增 `gamewiki/ooxml.py`（213 行，纯标准库）：从 OOXML 容器抽纯文本。
  - docx：`word/document.xml`，按 `w:p` 段落拼接 `w:t`，`w:tab`→制表符、`w:br/w:cr`→换行。
  - xlsx：`xl/workbook.xml` + `xl/_rels/workbook.xml.rels` 解析工作表名与顺序；`xl/sharedStrings.xml` 共享字符串；单元格按 `r` 属性还原列位置（稀疏单元格补空）；支持 `inlineStr`、`b`（布尔）；输出 `# 表名` + 制表符分隔网格。
- `config.py`：新增 `OOXML_PREVIEW_EXTENSIONS`、`MAX_OOXML_ARCHIVE_BYTES`(16 MB)、`MAX_OOXML_MEMBER_BYTES`(8 MB)、`MAX_OOXML_TEXT_CHARS`(20 万)；新增 `preview_kind(extension)` 供前端判断渲染方式。
- `server.py`：`_preview` 增 OOXML 分支；`OoxmlError` → 422；`/api/files` 每行附 `preview` 字段。
- `web/app.js`：删掉重复维护的 `previewableText`/`previewableImages` 两个扩展名集合，改读服务端 `preview`（原来 config.py 与 app.js 各存一份清单，加 .docx 时必然漏改前端）。
- `tests/test_ooxml.py`（176 行，14 个用例）：内存构造最小 OOXML 夹具，覆盖段落/制表符/共享字符串/稀疏单元格/多表顺序/空表/关系里 `..` 穿越/DOCTYPE/容器与成员超限/损坏容器/截断。
- 阈值依据实测而非拍脑袋：.xlsx 最大 533 KB、.docx 最大 25 KB、单成员解压后最大 0.43 MB、最大压缩比 36x。

## 实测结果
- 真实文件批量提取：**53/53 成功，0 失败**，累计 225,767 字符（均 4,259 字符/文件）；内容可用（P4G 日程表、PSNINE 成就奖杯表等均正确抽出）。
- HTTP 端到端：`.docx` 与 `.xlsx` 均返回 `type=text format=ooxml`；`.pdf` 仍 403。
- 测试套件 19 个全过。
- 不可预览缺口由 59 个文件降到 **6 个**（pdf 3 · xls 2 · rar 1）。

## 目标澄清（重要）
用户真实目标是**挂在网站上的公开 wiki**（公开互联网 + GitHub Pages + 已有域名 xiaomenghua.top），此前我误按项目文档写的「本地只读、仅监听本机」目标推进，优先级排错了。教训：动手前先确认最终交付形态。

## GitHub Pages 约束（官方文档核实）
- 发布站点硬限 **1 GB**，源仓库建议 1 GB；带宽软限 100 GB/月；构建 10 次/小时（Actions 流程不适用此限）；部署超时 10 分钟。
- **不支持任何服务端语言**（明确列出 PHP/Ruby/Python）→ 现有 `gamewiki/server.py` 架构无法上线，必须改为静态站点生成（索引与预览变成构建步骤）。
- 免费账号**只能从公开仓库发布**；私有仓库发布需 Pro/Team。即使私有仓库，站点本身始终公开。
- 自定义域名：apex 用 4 条 A 记录指向 GitHub IP；HTTPS 由 Let's Encrypt 自动签发。

## 部署体积实测（tmp/deploy_measure.py、tmp/thumb_probe.py）
资料库合计 213.94 MB = Pages 硬限的 **20.9%**（体积不是瓶颈）。
构成：图片 115.07 MB(53.8%) · `P5R攻略2.0.rar` 64.21 MB(30.0%) · pdf 32.50 MB(15.2%) · docx/xlsx 1.73 MB · 文本+xls 0.43 MB。
- `P5R攻略2.0.rar` 是 `P5R攻略2.0/`（120 文件已解压）的压缩备份，**纯冗余**，贡献 30% 体积零信息增量。
- 图片重编码实测（Pillow 12.3.0）：原样 115.07 MB → 仅超宽降采样(>1600px) 71.44 MB(62.1%) → 统一转 JPEG q82 **18.10 MB(15.7%)**。
- 两张巨图：`水基底地图.jpg` 8858×8858 19.98 MB → 1600px 0.53 MB；`油基底地图.jpg` 7480×5906 6.21 MB → 0.36 MB。
- docx/xlsx 抽文本：1.73 MB → 0.480 MB（27.8%）。
- 三方案部署体积：A 全原样 213.94 MB · B 文本+仅缩放 72.23 MB · B' 文本+JPEG 18.89 MB · C 仅目录 约 1-3 MB。

## 版权风险（必须由用户判断）
资料含 `New Horizon Walkthrough Official EN.pdf`（官方）、`转自PSNINE`（第三方转载）、`布丁大全`（汉化补丁合集）、`万代南梦宫启动器一键汉化工具` 等他人作品。公开仓库 + 公开站点 = 任何人可下载全部内容；A/B/B' 三者在版权上同类（都发布内容），只有 C（仅目录）不同。

## 环境备忘
- 本次会话 bash 环境 PATH 残缺（`ls`/`dirname` 均 command not found），且 PowerShell 的 stdout 不回传。可靠做法：用 PowerShell 执行并把输出重定向到 `tmp/*.txt`，再用 Read 读取；注意 PowerShell `>`/`Out-File` 默认产出 UTF-16，Python 侧写文件更稳。
- `unittest discover -s tests -t .` 需要 `tests/__init__.py`，已在 c99e0f1 补上。

## 下一步（目标修正后重排）
- **主任务：改造为静态站点**——现有 Python 服务无法上 Pages，需新增构建步骤（索引页 + 每份资料一页 + 搜索用 JSON）；`gamewiki/` 保留为本地工具与构建器。
- 待用户决定：内容上线范围（A 全原样 / B 文本+缩略图 / B' 文本+JPEG / C 仅目录）与版权把握。
- 建议顺手剔除 `P5R攻略2.0.rar`（冗余备份，64 MB）。
- 原分支降级：C 索引增量刷新、D 分类模型、F 仓库卫生、E 的误导性 503（改动小，可附带）。




