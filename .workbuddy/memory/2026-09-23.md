# 2026-09-23

## 源码入库

之前整条 wiki 轨道**一直没提交**（仓库里只有早期的 `gamewiki/`、`web/`、`legacy/`）。
本次分三个提交落地，工作区已干净：

| 提交 | 内容 |
| --- | --- |
| `835493b` | `feat(wiki)` mdrender / wiki / sources / ooxml / wiki 模板 / content（73 文件） |
| `d412398` | `feat(indexer)` 索引器静态托管模式（build.py + web/） |
| `540cc5d` | `feat(deploy)` deploy.py + README + .gitignore + 协作配置 |

`main` 现在**没有远端**，只有本地提交。

`.gitignore` 新增：`dist-wiki/`（构建产物，避免每次重建都往历史里塞 70 MB 二进制）；
`.workbuddy/*` + `!.workbuddy/memory/`（会话状态本地化，但保留 memory 以便跨设备续做）。

## 发布工具：gamewiki/deploy.py

用**持久克隆缓存**（`tmp/deploy-cache/`）持有 `gh-pages`，同步产物 → 提交 → 快进推送。
已在本地裸仓库上完整验证（首次/重复/单文件改动三种情形）。

### 两版方案的取舍（重要，别再走弯路）

1. **不用 `git subtree push`**：`dist-wiki/` 是构建产物且被忽略，subtree 要求它在 `main`
   上被跟踪，会导致每次重建都往 `main` 历史里再塞一份 70 MB。
2. **不用「造无父提交再强制推送」**（第一版实现）：实测**每次发布都重传全部 70 MB**。
   原因是无父提交时客户端手里没有远端旧提交的对象，打包时无法把它们排除——
   `size-pack` 从 68913 KB 涨到 137826 KB，正好翻倍。
   改成持久克隆后：重复发布「没有新提交」，单文件改动只推一个提交，`size-pack` 不再增长。
3. **远端是本地路径时必须转绝对路径**：缓存仓库的 cwd 和主仓库不同，相对路径在
   第二次发布会解析到缓存目录里去（`tmp/deploy-cache/tmp/xxx.git`）。
   这个 bug 只有拿本地裸仓库当远端测才会暴露，URL 形式的远端一辈子碰不到。

## 图片体积（待办，非阻塞）

`dist-wiki/` 70.49 MB：**118 张 PNG 占 60.17 MB**，60 张 JPG 占 8.89 MB。
最大单张 1.25 MB（`p5r/palace-madarame/05.png`）。

结论：**Pillow 其实是可用的（12.3.0）**，`max_edge`（1920/2200）也真的生效了；
体积大的原因是**输出沿用了素材的 `.png` 后缀**——PNG 装 2200px 的殿堂走法截图
（近照片内容）压缩极差。改成 WebP 预计能压到 1/4 左右（70 MB → 约 23 MB）。
页面加载体验会明显变好（一张走法地图页现在要拉 20 MB 图）。

## 部署阻塞点

`git` 本机**没有任何写凭据**：无 `~/.git-credentials`、无 `~/.ssh`，
凭据助手是 GCM 2.9.0 但没存过凭据。

**但发现可复用的凭据**：`C:\Users\Administrator\.workbuddy\mcp.json` 里的
`github-token` MCP server 有明文 `GITHUB_TOKEN`（PAT），连接器正是用它认证的，
对 `rangercd67` 有效。可用 env + `credential.helper` 注入给 `git push`，
不落到远端 URL 或 `.git/config` 里。

**安全观察**：该 PAT 以明文存放在 `mcp.json`，且本机无磁盘加密——建议改用
细粒度令牌并轮换。

## 账号现状

- GitHub 用户 `rangercd67`（2024-11-14 注册），`public_repos: 1`
- 唯一公开仓库是 `MoeCTF_2025_ranger`（fork），未开 Pages
- `xiaomenghua.top` 是 Cloudflare 托管的个人博客（Hexo 形态，`/2026/09/20/slug/`）
- `wiki.xiaomenghua.top` / `game-wiki.xiaomenghua.top` 均未解析
